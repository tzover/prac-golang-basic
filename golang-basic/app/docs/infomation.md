# Go command

```
go
```

# 別ファイルの実行

```
go build
go run main.go variable.go
go build main
go build -o main && ./main
```

> usage -> go build [-o output] [build flags] [packages]

# Format

```
go fmt  *.go
```
